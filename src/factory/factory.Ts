import ICheckbox from '../structure/ICheckbox';
import IConfiguration from '../structure/IConfiguration';

export default class CheckboxFactory {

    private config: IConfiguration;

    public build(node: INode): ICheckbox {
        
    }
}